.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Custom files

# Note: we can only comment out the entire .yarn folder since we strictly stick
# to nodeLinker: node-modules. If using pnp, the folder contains important info
.yarn
